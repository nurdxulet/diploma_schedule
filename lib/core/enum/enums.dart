enum QuestionConuntType { notAnswered, answered, selected, right, wrong }

enum AnswerType { right, wrong, notSelected }

enum TestType { diagnostic, variantAnalysis }

enum TestPath { fromProfileTest, fromOqysai }
